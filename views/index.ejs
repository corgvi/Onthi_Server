<!DOCTYPE html>
<html>
<head>
    <title>Thi Thu</title>
    <link rel='stylesheet' href='/stylesheets/style.css'/>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
</head>
<body>
<h1>Insert</h1>
<hr>
<div class="container">
    <form action="/search" method="post" enctype="application/x-www-form-urlencoded" class="form-inline my-2 my-lg-0"
          style="margin-left: 10px">
        <input type="text" class="form-control mr-sm-2" placeholder="Tìm kiếm theo mã sản phẩm" name="timKiem"
               class="form-control"
               style="width: 90%">
        <button class="btn btn-outline-success my-2 my-sm-0" style="width: 8.7%; height: 40px">Tim kiem</button>
    </form>
    <form class="d-flex flex-column" action="/create" enctype="multipart/form-data" method="post">

        <input class="my-3" type="text" name="ma" placeholder="Mã oto">
        <input class="my-3" type="text" name="nhanhieu" placeholder="Nhãn hiệu">
        <input class="my-3" type="text" name="namsx" placeholder="Năm sản xuất">
        <input class="my-3" type="text" name="giagoc" placeholder="Giá gốc">
        <input class="my-3" type="text" name="giaban" placeholder="Giá bán">
        <input class="my-3" type="file" name="avatar" multiple>

        <button class="my-3" type="submit">OK</button>
    </form>
    <!--search-->

</div>
<hr>

<h1>Uploaded</h1>
<div>

    <% if(items != null){ %>
        <% for(var i = 0; i < items.length;i++) { %>
            <div class="item">
                <% for(var j = 0; j < items[i].avatar.length;j++) { %>
                    <img src="images/<%= items[i].avatar[j] %>" style=" width: 45%;height: 400px;">
                <% } %>
                <h5><%= items[i].nhanhieu %></h5>
                <p><%= items[i].ma %></p>
                <p><%= items[i].namsx %></p>
                <p><%= items[i].giagoc %></p>
                <p><%= items[i].giaban %></p>
                <div>
                    <button style="background: darkorange">
                        <a class="text-decoration-none text-black fw-bold"
                           href="/delete/?id=<%= items[i]._id %>">Delete</a>
                    </button>
                    <button>
                        <a class="text-decoration-none text-black fw-bold" href="/updateForm/?id=<%= items[i]._id %>">Update</a>
                    </button>
                    </a>
                </div>
            </div>
        <% } %>
    <% } %>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-A3rJD856KowSb7dwlZdYEkO39Gagi7vIsF0jrRAoQmDKKtQBHUuLZ9AsSv4jD4Xa"
        crossorigin="anonymous"></script>
</body>
</html>
